    // fscanf[/ b]([b] fptr, "%d%s%f", &stock, item, &price); /* read data */
    // printf("%d  %s  %4.2f\n", stock, item, price);
    // while ((c = getc(fptr)) != EOF) /* read the rest of the file */
    //     printf("%c", c);